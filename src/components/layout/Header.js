import tw from 'twin.macro';

const Header = tw.header`bg-white px-12 py-4 h-20 border-b text-xl text-gray-400 uppercase`;

export default function HeaderComponent() {
  return <Header>Logo</Header>;
}
